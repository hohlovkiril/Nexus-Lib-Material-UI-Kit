import {
  MobileStepper,
  Stepper,
  Step,
  StepButton,
  StepConnector,
  StepContent,
  StepIcon,
  StepLabel,
  styled,
} from '@mui/material'
import {
  MobileStepperProps,
  StepperProps,
  StepProps,
  StepButtonProps,
  StepConnectorProps,
  StepContentProps,
  StepIconProps,
  StepLabelProps,
} from './Stepper.types'

export const StyledMobileStepper = styled(MobileStepper)<MobileStepperProps>(() => ({

}))

export const StyledStepper = styled(Stepper)<StepperProps>(() => ({

}))

export const StyledStep = styled(Step)<StepProps>(() => ({
  
}))

export const StyledStepButton = styled(StepButton)<StepButtonProps>(() => ({
  
}))

export const StyledStepConnector = styled(StepConnector)<StepConnectorProps>(() => ({
  
}))

export const StyledStepContent = styled(StepContent)<StepContentProps>(() => ({
  
}))

export const StyledStepIcon = styled(StepIcon)<StepIconProps>(() => ({
  
}))

export const StyledStepLabel = styled(StepLabel)<StepLabelProps>(() => ({
  
}))