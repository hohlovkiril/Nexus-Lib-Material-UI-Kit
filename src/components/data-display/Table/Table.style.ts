import { styled, TableContainer, Table, TableHead, TableSortLabel, TableBody, TableFooter, TableRow, TableCell, TablePagination} from "@mui/material";

export const StyledTableContainer = styled(TableContainer)(() => ({

}))

export const StyledTable = styled(Table)(() => ({

}))

export const StyledTableHead = styled(TableHead)(() => ({
  
}))

export const StyledTableSortLabel = styled(TableSortLabel)(() => ({

}))

export const StyledTableBody = styled(TableBody)(() => ({

}))

export const StyledTableFooter = styled(TableFooter)(() => ({

}))

export const StyledTableRow = styled(TableRow)(() => ({

}))

export const StyledTableCell = styled(TableCell)(() => ({

}))

export const StyledTablePagination = styled(TablePagination)(() => ({

}))